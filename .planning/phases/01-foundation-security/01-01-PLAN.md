---
phase: 01-foundation-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - src/router.tsx
  - src/client.tsx
  - src/routes/__root.tsx
  - src/routes/index.tsx
  - src/styles/app.css
  - .env.example
  - .gitignore
  - app.config.ts
autonomous: true
user_setup:
  - service: supabase
    why: "Database, auth, and storage backend"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: VITE_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon/public key"
    dashboard_config:
      - task: "Create new Supabase project for Petra Pinterest"
        location: "Supabase Dashboard -> New Project"
      - task: "Enable Google OAuth provider"
        location: "Supabase Dashboard -> Authentication -> Providers -> Google"

must_haves:
  truths:
    - "TanStack Start dev server runs without errors on localhost:3000"
    - "TypeScript compilation succeeds with no errors"
    - "Tailwind CSS styles are applied to pages"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "@tanstack/react-start"
    - path: "vite.config.ts"
      provides: "Vite + TanStack Start configuration"
      contains: "tanstackStart"
    - path: "src/routes/__root.tsx"
      provides: "Root route with HTML document structure"
      exports: ["Route"]
    - path: "src/routes/index.tsx"
      provides: "Landing page route"
      exports: ["Route"]
  key_links:
    - from: "vite.config.ts"
      to: "@tanstack/react-start/plugin/vite"
      via: "tanstackStart plugin import"
      pattern: "tanstackStart\\(\\)"
    - from: "src/router.tsx"
      to: "src/routeTree.gen"
      via: "generated route tree import"
      pattern: "routeTree"
---

<objective>
Initialize TanStack Start project with all core dependencies for the Pinterest scheduling dashboard.

Purpose: Establish the foundation that all subsequent plans build upon - a working TanStack Start application with proper TypeScript, Tailwind CSS, and project structure.

Output: A functional TanStack Start skeleton that runs on localhost:3000 with proper dev tooling.
</objective>

<execution_context>
@/Users/cweissteiner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cweissteiner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/01-foundation-security/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize TanStack Start project with dependencies</name>
  <files>package.json, tsconfig.json, vite.config.ts, app.config.ts, .gitignore, .env.example</files>
  <action>
Create a new TanStack Start project manually (not using CLI to ensure full control):

1. Create package.json with these exact dependencies:
   - @tanstack/react-start (latest)
   - @tanstack/react-router (latest)
   - @tanstack/react-router-devtools (dev)
   - @tanstack/react-query (latest)
   - @supabase/supabase-js (latest)
   - react, react-dom (^19.0.0)
   - vite, @vitejs/plugin-react
   - vite-tsconfig-paths
   - typescript (^5.0.0)
   - tailwindcss, postcss, autoprefixer
   - @tailwindcss/vite (for Tailwind v4)
   - clsx, tailwind-merge

   Scripts:
   - "dev": "vinxi dev"
   - "build": "vinxi build"
   - "start": "vinxi start"

2. Create tsconfig.json with:
   - "moduleResolution": "bundler"
   - "jsx": "react-jsx"
   - "strict": true
   - paths: { "~/*": ["./src/*"] }
   - include: ["src/**/*", "*.config.ts"]

3. Create vite.config.ts:
   - Import tanstackStart from @tanstack/react-start/plugin/vite
   - Import viteReact from @vitejs/plugin-react
   - Import tailwindcss from @tailwindcss/vite
   - Import tsConfigPaths from vite-tsconfig-paths
   - Order: tsConfigPaths(), tanstackStart(), tailwindcss(), viteReact()
   - Set server.port to 3000

4. Create app.config.ts for TanStack Start:
   - Export default defineConfig with server preset

5. Create .gitignore with node_modules, dist, .vinxi, .env, .env.local

6. Create .env.example with placeholder vars:
   - VITE_SUPABASE_URL=your-project-url
   - VITE_SUPABASE_ANON_KEY=your-anon-key
  </action>
  <verify>
Run `npm install` - should complete without errors.
Run `npm run dev` - should start (will fail on missing routes, that's expected).
  </verify>
  <done>
package.json exists with all required dependencies, tsconfig.json configured for TanStack Start, vite.config.ts loads all plugins correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create core application structure and routes</name>
  <files>src/router.tsx, src/client.tsx, src/routes/__root.tsx, src/routes/index.tsx, src/styles/app.css</files>
  <action>
Create the TanStack Start application structure:

1. Create src/router.tsx:
   - Import createRouter from @tanstack/react-router
   - Import routeTree from ./routeTree.gen
   - Export getRouter function that returns createRouter({ routeTree, scrollRestoration: true })

2. Create src/client.tsx:
   - Import StartClient from @tanstack/react-start/client
   - Import hydrateRoot from react-dom/client
   - Wrap StartClient in StrictMode and hydrate to document

3. Create src/styles/app.css with Tailwind v4 import:
   - @import "tailwindcss";
   - Add CSS variables for shadcn/ui compatibility (--background, --foreground, --primary, etc.)
   - Use neutral color palette as base

4. Create src/routes/__root.tsx:
   - Import createRootRoute from @tanstack/react-router
   - Import HeadContent, Outlet, Scripts from @tanstack/react-router
   - Set up head() with meta charset, viewport, title "Petra Pinterest"
   - Link to app.css stylesheet
   - Create RootDocument component with html, head (HeadContent), body (children, Scripts)
   - Include TanStackRouterDevtools in development only
   - Export Route using createRootRoute

5. Create src/routes/index.tsx:
   - Import createFileRoute from @tanstack/react-router
   - Create simple landing page component with "Petra Pinterest" heading
   - Style with Tailwind: centered content, nice typography
   - Add placeholder "Sign in with Google" button (non-functional for now)
   - Export Route using createFileRoute('/')
  </action>
  <verify>
Run `npm run dev` - should start without errors on localhost:3000.
Visit localhost:3000 - should show landing page with Tailwind styles applied.
  </verify>
  <done>
TanStack Start dev server runs successfully, landing page renders with Tailwind CSS styling, no TypeScript errors.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. `npx tsc --noEmit` passes with no TypeScript errors
3. localhost:3000 shows styled landing page
4. Browser devtools shows no console errors
5. TanStack Router Devtools visible in development
</verification>

<success_criteria>
- TanStack Start application skeleton is functional
- All dependencies install correctly
- TypeScript compilation succeeds
- Tailwind CSS styles are working
- Landing page renders at localhost:3000
- Project structure matches TanStack Start conventions
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-security/01-01-SUMMARY.md`
</output>
